var UserSendSave = function(cnf) {
    this.username  = cnf.username,
    this.userMoney = cnf.money,
    this.date      = cnf.date,
    this.gameData  = cnf.gameData

};

module.exports = UserSendSave;